---
description: Report current build state and the next best action for the Student Founder project.
agent: ship-director
---

Load the `zero-to-deploy` skill. Read `BUILD_PROGRESS.md` and reconcile it against the actual repo
state (git, `backend/`, `frontend/`, running apps). Report concisely:
1. Where the project stands against the blueprint Part 5 order (use the checklist).
2. What is verified/done and what is not.
3. The single next best action, with the concrete subtasks and the "Done when" check for that step.
$ARGUMENTS